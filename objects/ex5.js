const prompt = require('prompt-sync')();
let aa = [
    {
        title: "puwer",
        names: "ayoub",
        age: 22
    }
]
function array(aa) {

}
for (let b = 0; b < aa.length; b++) {
    console.log(`the title : ${aa[b].title} 
the names is  : ${aa[b].names} 
the title : ${aa[b].age}`);
}